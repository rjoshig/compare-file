# Dashboard nav toggles (Vite build-time env; restart `npm run dev` after edits).
# Both are SHOWN by default. Set a value to "false" to hide that nav item.
VITE_SHOW_RESULTS=true
VITE_SHOW_RUN_HISTORY=true
