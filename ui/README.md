# UI (Phase 3 placeholder)

This directory is reserved for the Phase 3 Vue.js 3 + Vite frontend.

Actual scaffolding (`npm create vite@latest`, components, views, router,
services) lands in Phase 3 — see `../docs/phase-3.md`. Until then this
directory intentionally contains only this README.

Planned layout:

```
ui/
├── package.json
├── vite.config.js
├── index.html
├── src/
│   ├── main.js
│   ├── App.vue
│   ├── router/
│   ├── views/
│   ├── components/
│   ├── services/
│   └── store/
└── public/
```
