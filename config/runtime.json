{
  "$comment": "Run-wide runtime knobs. Per-file sort metadata (input_sorted, order, key_type) lives on each layout_file_*.json::sort block now (ADR-033). parallel_workers is the default worker count for the parallel pipeline; the CLI --workers flag overrides per-invocation (ADR-028). partition_strategy currently supports only 'equal_count'.",

  "hash_method": "blake2b",
  "blake2b_digest_size": 16,

  "sort_temp_dir": "/tmp/segment_compare",

  "parallel_workers": 8,
  "chunk_size": 10000,
  "partition_strategy": "equal_count"
}
