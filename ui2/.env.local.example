# Copy to .env.local to override defaults.

# Where the FastAPI backend is reachable. The Next.js dev server proxies
# /api/* to this origin (see next.config.mjs), so the browser never needs CORS.
SEGCMP_API_URL=http://127.0.0.1:8000
